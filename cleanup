rm -f confdefs.h config.log config.status \
	src/*.o src/*.so src/*.a src/*.d src/*.dll src/*.rc \
	src/Makedeps \
	build/Rcpp.pdf \
	src/symbols.rds
	
rm -rf	autom4te.cache src-*

find . -name \*~ -exec rm {} \;
find . -name \*.flc -exec rm {} \;
